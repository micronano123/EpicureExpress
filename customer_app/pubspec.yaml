name: customer_app
description: "A new Flutter project."

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.7.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  http: ^0.13.6  # API calls
  shared_preferences: ^2.0.15  # Local storage
  google_maps_flutter: ^2.1.8  # Order tracking
  firebase_auth: ^4.4.0  # User authentication
  cloud_firestore: ^4.3.0  # Database
  flutter_stripe: ^9.2.0  # Payments

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true